# Round 2 Merge Plan — Sync friend's new web changes into the mobile-API branch

**Status:** Planning complete (deep analysis + a real trial merge already run and verified). Execution not started — waiting for go-ahead.

**Scope discipline (non-negotiable):** This merge must **not** alter the web developer's logic, blade views, controllers, or feature behavior in any way. My job was only to build the mobile-app REST API layer (`app/Http/Controllers/Api/**`, `routes/api.php`). Any conflict must be resolved by keeping the web dev's code exactly as authored and only adjusting on the API side, or — in the one case where that's not possible (§4) — making a purely mechanical fix (de-duplicating a line) that changes zero behavior. This keeps the web dev's mental model of his own code intact for future client-requested changes.

---

## 1. Background — this is Round 2

Round 1 already happened (see `docs/MOBILE_API_MERGE_PLAN.md`, now historical/superseded): mobile API work was merged with the web code as of PR #79 (`2e09e01`), producing branch `merge/mobile-api` (merge commit `b20a890`).

Since then:
- **You** kept building on `merge/mobile-api` — more API controllers, currently **uncommitted** in the working tree.
- **The web developer** pushed two more PRs to `origin/main`: **PR #80** and **PR #81** (5 commits, `04f3e40` → `cf41d1f`), which `merge/mobile-api` does not have yet.

This round syncs those PR #80/#81 changes into `merge/mobile-api`, in `J:\laragon\www\eye_care_new_clone\eye-saas-hms` only. `eye_care_new_clone_2` was not needed as a source — it points at the same GitHub repo (`origin`), so `git fetch origin` already has everything it has.

---

## 2. Current git state (verified)

| Ref | Points at | Notes |
|---|---|---|
| `merge/mobile-api` (current branch) | `b20a890` | Round-1 merge result + your uncommitted API work on top |
| `origin/main` (fetched) | `cf41d1f` | Web dev's latest, includes PR #80 + #81 |
| Merge base | `2e09e01` | PR #79 — the point both sides last agreed on |

Your uncommitted work right now (verified via `git diff --stat` / `git status`):

**Modified (tracked):**
- `app/Http/Controllers/Api/ClinicalQueueApiController.php`
- `app/Http/Controllers/Api/DashboardController.php`
- `app/Http/Controllers/Api/MastersApiController.php`
- `app/Http/Controllers/Api/PatientApiController.php`
- `app/Http/Controllers/Api/PatientHistoryApiController.php`
- `app/Http/Controllers/Api/SuperAdmin/TenantApiController.php`
- `routes/api.php`
- `vendor/composer/autoload_classmap.php`, `vendor/composer/autoload_static.php` (auto-generated, regenerated by composer — not hand-written)

**New (untracked):**
- `app/Http/Controllers/Api/DoctorDashboardApiController.php`
- `app/Http/Controllers/Api/FocApiController.php`
- `app/Http/Controllers/Api/RoleApiController.php`
- `docs/MOBILE_API_MERGE_PLAN.md` (Round-1 doc, historical)

**100% confined to `app/Http/Controllers/Api/**` and `routes/api.php`.** Not one file overlaps with a web-facing controller, blade view, or model.

---

## 3. What the web dev changed in Round 2 (PR #80 + #81, `2e09e01..origin/main`)

35 files, +3875/−842 lines, zero dependency changes (`composer.json`/`lock` untouched). Three logical chunks:

**a) New "Medicine Master" feature (PR #80, commit `04f3e40`)** — global medicine catalog, entirely new:
- New: `MedicineMasterController`, `MedicineMasterImport`, `MedicineMasterSampleExport`, `MedicineTenantSync` service, 5 new `Platform` models, 1 new migration (`create_master_medicine_tables`), 4 new seeders, 1 new blade view, `SeedTenantDefaultMasters` job heavily rewritten.
- Touches `database/seeders/DatabaseSeeder.php` (+4 lines) — **the one real conflict, see §4.**

**b) Dashboard / patient-history rework (PR #81, commit `55b4dc9`)** — pure web UI/controller work:
- `Hospital/Dashboard/DashboardController.php`, `Hospital/Patient/PatientController.php`, `Hospital/Patient/PatientHistoryController.php` rewritten.
- Several blade views rewritten/added (`dashboard/index`, `doctor_history`, `partner_history`, new partials).
- `patients/index.blade.php` — 477 lines changed (this is the *same file* your Round-1 work touched with a 1-line link fix — see §4.2 for why this is not actually a problem).
- `AppServiceProvider.php` — added `URL::forceScheme('https')` outside `local` env (see §5, informational).

**c) Small formatting/consistency commit (`f82f2f4`, "history")** — `DashboardController`, `SecondaryExamController`, `PatientHistoryController`, two blade files. Confirmed **cosmetic only** (spacing/arrow-function style, e.g. `fn ($x)` → `fn($x)`) — no logic changes. Verified specifically for `SecondaryExamController` since it calls `ExaminationService`, which your Round-1 work also modified — no interaction risk (see §5).

**Zero file-level overlap** between this list and your uncommitted API work (§2). Different namespaces (`Hospital\*`, `SuperAdmin\*`, `Platform\*` vs. your `Api\*`), different route file (`routes/web.php` vs. your `routes/api.php`).

---

## 4. Conflicts — verified by an actual trial merge, not guesswork

A real `git merge origin/main --no-commit` was run on a disposable throwaway branch (not on `merge/mobile-api`, not on `main`) to see the *actual* merge result rather than predict it. Result: **exactly one conflicting file** out of 35. Everything else — including the 33 web-only files — auto-merged cleanly.

### 4.1 `database/seeders/DatabaseSeeder.php` — the only conflict

- In Round 1, your seeder additions **moved** `MasterHnoSeeder::class` up into a "Clinical masters" section and removed it from the tail of the list.
- In Round 2, the web dev added 4 new lines *right after* the tail occurrence of `MasterHnoSeeder::class` — but that tail occurrence no longer exists in the merged file (it's now higher up), so git can't find the exact context and flags a conflict.
- **This is not a logic disagreement, just a duplicate-line artifact.** Resolution (confirmed by inspecting both sides):

```php
// ── Medicine masters ──────────────────────────────────────
DosageSeeder::class,
MedicineTypeSeeder::class,
MedicineCategorySeeder::class,
MedicineRouteSeeder::class,
MasterDosageSeeder::class,
MasterMedicineTypeSeeder::class,
MasterMedicineCategorySeeder::class,
MasterMedicineRouteSeeder::class,
```
i.e. keep both sides' new seeders, drop the incoming `MasterHnoSeeder::class` line only (it's already present earlier in the file). No seeder's own code is touched — only which line of `DatabaseSeeder.php` calls it, and only to remove an exact duplicate. This does not change what the web dev's `MasterMedicineMaster*` seeders do.

### 4.2 `resources/views/hospital/patients/index.blade.php` — checked, not actually a conflict

Your Round-1 diff for this file compared to the merge base came back **empty** — meaning your 1-line `patient_ids` link change either never landed in this file during the Round-1 merge resolution, or was superseded already. Either way, `merge/mobile-api`'s current version of this file is identical to the web dev's pre-Round-2 version, so the Round-2 rewrite (477 lines) applies as a normal clean update with no conflict (confirmed in the trial merge — this file merged automatically, no conflict marker). Nothing of yours is at risk here because there is nothing of yours left in this file to lose.

**No other files conflicted.** Confirmed via the trial merge's full file list (all 33 other web-only files applied as clean `A`/`M` — new-file-add or clean-modify, no `UU`).

---

## 5. Reviewed, no action needed (informational only)

- **`AppServiceProvider.php`** — web dev added `URL::forceScheme('https')` when not in `local` env. Your side has zero changes to this file, so it merges in untouched. Worth knowing: this will make Laravel's `url()`/`route()` helpers emit `https://` in staging/production, which also affects any absolute URLs your API might return (e.g. file/image links). Not a conflict, not something to change — just something to be aware of if an API response ever looks like it "changed" a URL scheme after this merge.
- **`SecondaryExamController.php`** — touched by the web dev in Round 2 (uses `ExaminationService`, which your Round-1 work changed from overwrite to `array_merge` semantics for `savePrimary`/`saveSecondaryExam`). Diffed line-by-line: the Round-2 change here is pure code-style reformatting (brace placement, `fn ($x)` spacing) — the actual calls into `ExaminationService` are byte-identical. No new interaction with your merge-logic change.
- **No new dependencies** — `composer.json`/`composer.lock` untouched by Round 2, so `composer install` is not required, though running it is harmless and free to double check.
- **New migration** (`2026_07_06_000001_create_master_medicine_tables.php`) — new table, no timestamp collision with anything on your side (you added no migrations in Round 1 either — verified).
- **New routes** — all 21 new routes in Round 2 are in `routes/web.php` under `/medicine-master`, named `medicine-master.*`. Your routes all live in `routes/api.php`. No name or path collisions possible.

---

## 6. Step-by-step execution (not started — waiting for your go-ahead)

All of this happens on the `merge/mobile-api` branch in `J:\laragon\www\eye_care_new_clone\eye-saas-hms`. `main` is not touched at any point in this section.

1. **Checkpoint your current uncommitted work** as a real commit on `merge/mobile-api`, so there's a clean rollback point:
   ```
   git add -A
   git commit -m "Mobile API: dashboard/masters/patient-history/clinical-queue/tenant additions"
   ```
2. **Merge in the web dev's Round 2 changes:**
   ```
   git merge origin/main
   ```
3. **Resolve the one conflict** in `database/seeders/DatabaseSeeder.php` exactly as shown in §4.1 (drop the duplicate `MasterHnoSeeder::class` line from the incoming side, keep everything else from both sides), then:
   ```
   git add database/seeders/DatabaseSeeder.php
   git commit
   ```
4. **Sanity pass** (no DB/state-changing commands run without your OK):
   ```
   composer install          (harmless no-op here, confirms nothing missing)
   php artisan config:clear && php artisan route:clear && php artisan view:clear
   php artisan route:list --path=api     (confirm all mobile routes still register, no collisions)
   php artisan migrate:status            (confirm the new medicine-master migration shows as pending, nothing else broken)
   ```

---

## 7. Testing checklist (must pass before this is considered done)

### 7.1 Web regression (this is the critical "did we break the web dev's work" check)
- [ ] New Medicine Master admin page loads, CRUD (dosages/types/categories/routes/medicines) works, import/export sample works
- [ ] Dashboard (`hospital/dashboard/index.blade.php`) loads correctly for doctor/receptionist roles
- [ ] Doctor history / partner history pages load
- [ ] Patients index page loads, listing/search/pagination works
- [ ] Patient history page(s) load correctly
- [ ] Primary exam save flow still works (touched by the "history" formatting commit)
- [ ] Secondary exam save/edit still works, **and previously-saved sections still persist after a partial re-save** (this is the direct regression test for your Round-1 `ExaminationService` merge-logic change, now doubly important since the web dev's controller for this file changed too)
- [ ] Run `php artisan migrate:fresh --seed` on a throwaway/test DB — confirms the merged `DatabaseSeeder.php` runs start-to-finish, including the 4 new medicine-master seeders in the right order, no FK errors

### 7.2 Mobile API smoke test
- [ ] Auth/login/token flow
- [ ] Patient API (list/create/update/history)
- [ ] Dashboard API, Clinical Queue API, Masters API, Tenant API — one request each, confirm no fatal errors after the merge
- [ ] New untracked controllers (`DoctorDashboardApiController`, `FocApiController`, `RoleApiController`) — confirm routes registered and respond

---

## 8. Rollback plan

Everything happens on `merge/mobile-api`. Both `main` (local, in this folder) and `origin/main` are never touched until you explicitly say to push. If anything looks wrong after the merge, `git reset` back to the checkpoint commit from step 1 (or just delete/recreate the branch from `b20a890`) — zero risk to any working branch.

---

## 9. Explicitly out of scope for this merge

- No blade view content changes.
- No changes to `Hospital\*`, `SuperAdmin\*`, or `Platform\*` namespace controllers/models/services beyond what git auto-merges mechanically.
- No changes to the web dev's seeder *classes* — only the one duplicate *reference* line in `DatabaseSeeder.php`, per §4.1.
- No push to `origin` / no merge into `main` until you review the result and say go (§10 in the old Round-1 doc had this same gate — same rule applies here).

---

## 10. Known unrelated issue (carried over from Round 1, still true)

The `origin` remote URL has a GitHub Personal Access Token embedded in plaintext (`https://ghp_...@github.com/...`). Unrelated to this merge, but should be rotated/revoked and replaced with a credential manager at some point.
